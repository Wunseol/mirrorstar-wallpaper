# 附录 B：版本历史

> [← 返回索引](./README.md)

本附录记录 v1.0 → v3.5.3 的主要变更日志，涵盖 5 轮审计修复（v3.0→v3.5）及各迭代的实施记录。

---

## v1.0 → v2.0

1. **全面重新审计所有模块源码**：基于 2026-06-22 对项目全部源码的深度审计，更新文档内容。
2. **修正代码行数、文件清单、测试数量等事实性数据**：
   - config 模块：961 行 → 1270 行，19 测试 → 23 测试
   - wallpaper 模块：3764 行/7 文件 → 6211 行/13 文件，0 测试 → 39 测试
   - audio 模块：127 行 → 177 行，0 测试 → 2 测试
   - ipc 模块：836 行 → 915 行，30 测试
   - process 模块：192 行 → 295 行，0 测试 → 6 测试
   - main.ts：775 行 → 403 行（v2.1 后大幅重构缩减）
   - 全局静态量：6 个 → 10 个（修正）
3. **将 48 个已修复问题标记为"✅ 已修复"**：在各章节问题清单中明确标注修复状态和修复说明。
4. **移除已修复问题的 vibecoding 提示词**：config、wallpaper、audio/ipc/process 模块全部移除；desktop、Tauri、wp-proc、前端、构建章节仅保留未修复问题的提示词。
5. **更新优化优先级矩阵**：仅保留 10 个未修复问题（v1.0 的 58 个问题中 48 个已修复，修复率 83%）。
6. **更新实施路线图**：标注各阶段完成状态（Phase 1 基本完成、Phase 2 已完成、Phase 3 部分完成、Phase 4-5 待启动）。
7. **更新度量指标**：反映当前实际值（单元测试 45 → 113，Critical Bug 6 → 0，热重载失效 → 正常，音量控制不工作 → 正常，开发模式端口不匹配 → 正常）。
8. **新增 wallpaper 子模块文件清单表、ipc client.rs 文件说明、config settings.rs 文件说明**。
9. **修正 wallpaper 模块文件清单**：v1.0 文档提及的 renderer.rs 实际不存在，v2.0 修正为 7 个文件清单。
10. **新增附录 A 和附录 B**：已修复问题汇总和审计变更日志。

---

## v2.0 → v2.1（2026-06-23）

1. **10 个未修复问题全部修复**：审计问题修复率从 83% 提升至 100%（58/58）。
2. **desktop unsafe Send/Sync 修复**：移除冗余 `unsafe impl Sync for DesktopIntegrator`，跨线程访问通过 `Arc<Mutex<DesktopIntegrator>>` 保护。
3. **Tauri 全局静态量优化**：合并 FULLSCREEN_PAUSE_SENDERS + POWER_PAUSE_SENDERS 为 SHARED_PAUSE_SENDERS，合并 FULLSCREEN_CONFIG + POWER_CONFIG 为 SHARED_CONFIG，TRAY_PAUSED 和 TRAY_PAUSE_RESUME_ITEM 移入 AppState，10→7 个。
4. **窗口关闭竞态修复**：添加 MAIN_WINDOW_CLOSING AtomicBool 标志位，替代 50ms sleep 魔数。
5. **COM 初始化错误处理改进**：使用 `RPC_E_CHANGED_MODE` 常量替代硬编码值，改用 match 表达式 + tracing 日志。
6. **前端全局状态封装**：selectedDisplayId、allWallpapers、currentPreviewId、statusTimer 封装为 appState 对象。
7. **wp-proc ipc_thread 改进**：JSON 反序列化失败改为 tracing::error + continue，不再 PostQuitMessage 终止进程。
8. **wp-proc build_url 重写**：剥离 `\\?\` 前缀、正确处理 UNC 路径、添加 percent_encode_path 辅助函数。
9. **前端未使用 JS 依赖清理**：移除 @tauri-apps/plugin-autostart 和 @tauri-apps/plugin-dialog。
10. **capabilities asset 权限收紧**：assetProtocol.scope 声称收紧为 `["$APPDATA/mirrorstar/**", "$HOME/**"]`，但 v3.0 实测仍为 `["**"]`（⚠️ 未实际收紧）。
11. **wp-proc 新增 9 个单元测试**：parse_rect 5 + build_url 3 + percent_encode_path 1，单元测试总数 104→113。

---

## v2.1 → v3.0（2026-07-03）

1. **全面代码复审**：基于 `refresh-review-and-refactor-docs-2026-07-03` spec，对 3 个 Rust crate + 前端 + 构建基础设施进行逐文件审查，产出 findings/00-06 共 7 份报告。
2. **数据勘误**：修正 v2.1 文档中的数据偏差——#[ignore] 36→18（v2.1 高估 2 倍）、src-tauri lib.rs 196→310 行、wp-proc 1645→1665 行、wallpaper 3764→6211 行、main.ts 775→403 行、main.css 555→581 行、index.html 115→117 行。
3. **状态勘误**：C-112（gif.rs _e 命名）和 C-113（ipc 轮询无退避）更正为"未修复"；C-107（let _ = 忽略错误）更正为"部分修复"。
4. **安全发现**：新增 WP-001 [P2 SEC-004]——wp-proc 协议白名单大小写敏感绕过（`JavaScript:` 变体可绕过 `starts_with("javascript:")` 检测），应优先修复。
5. **assetProtocol.scope 勘误**：v2.1 声称已收紧，但 v3.0 实测仍为 `["**"]`（⚠️ 未实际收紧，需重新修复）。
6. **input 模块引用清理**：input 模块已在 cycle4a 删除，v3.0 全部标注"已删除"。
7. **新增 dead deps 清理建议**：anyhow、raw-window-handle（Rust）、@vitest/ui（前端）为未使用依赖。
8. **新增 release.yml 改进建议**：添加 `cargo fmt --check` 和 `cargo audit` 步骤。
9. **文档导航完善**：docs/README.md 新增 优化文档/ 和 测试报告/ 目录链接。
10. **架构文档同步**：03-模块设计-Module-Design.md、05-依赖与数据流-Dependency-and-Data-Flow.md、07-暂停恢复机制-Pause-Resume.md、02-系统架构-System-Architecture.md、quality.md 中行数、位置等全部更正。

---

## v3.0 → v3.1（2026-07-04）

基于 `fix-v3-review-findings-2026-07-04` spec，完成 13 个 Task，修复 24 项问题。

1. **安全修复**：WP-001 协议白名单大小写敏感绕过（webview.rs `eq_ignore_ascii_case`，+6 测试）；assetProtocol.scope 从 `["**"]` 收紧为 `["$APPDATA/mirrorstar/**/*", "$HOME/**/*"]` 静态白名单。
2. **v2.1 遗留修复**：C-112（gif.rs `_e` 命名）、C-113（ipc/client.rs Backoff 指数退避 10ms→100ms，+8 测试）、C-107（22 处 `let _ =` 改为显式错误处理）。
3. **代码质量修复**：N-001~N-010（DRY 提取、dead code 文档、WorkerW 预检、set_wallpaper 委托、toggle_mute_atomic、protocol.rs 重命名、WebM/HTML magic detection、escape_windows_arg 返回 Result）；WP-002~WP-010（命名、JoinHandle、坐标校验、错误路径测试、GetModuleHandleW 缓存、parse_rect 错误传播等）。
4. **WP-008 简化方案**：管道安全属性因 Cargo.toml 约束无法启用 Win32_Security_Authorization feature，采取注释 + TODO 简化方案（v3.3 完整实现）。
5. **构建基础设施改进**：移除 dead deps（anyhow、raw-window-handle、@vitest/ui）、合并 Win32_System_Pipes 重复依赖、添加 MIT LICENSE 全文、release.yml 添加 cargo fmt --check + cargo audit 步骤。
6. **测试缺口填补**：wallpaper/manager.rs 610L/0T → 1336L/39T（+39 测试）、wp-proc main.rs 145L/0T → 322L/10T（+10 测试）。
7. **验证结果**：cargo test --workspace **374 passed / 0 failed / 47 ignored**（mirrorstar-core 211→272 +61、wp-proc 41→86 +45）；clippy 0 warnings；fmt 通过；npm test 86 passed；build 成功。

---

## v3.1 → v3.2（2026-07-04）

基于 `deep-review-src-tauri-frontend-2026-07-04` spec，深度审查 src-tauri（12 文件/2262 行）与前端（15 源文件 + 5 测试文件/3134 行），产出 findings/03（src-tauri，18 个问题）+ findings/04（前端，15 个问题），合计 33 个新发现问题，全部已处理（28 修复 + 5 TODO，后者 v3.3 修复）。

1. **Bug #3 修复（FE-001 P2）**：静音切换无法取消的根因——前端 displayId 空字符串传递不一致。采用前端 + 后端 + core 三层协同修复：前端 ipc.ts 5 个函数统一 `displayId || null`；后端 wallpaper.rs 5 个命令改为 `Option<String>` + `resolve_display_id`；core manager.rs 新增 `first_active_display_id()`。+7 ipc 测试 + 2 集成测试。
2. **src-tauri 审查修复（ST-001~ST-018）**：18 个问题全部处理（14 修复 + 4 TODO）。关键：ST-004 pause/resume DRY 重构；ST-007 测试缺口填补（纯函数提取 + 11 个非 ignored 测试）；ST-010 assetProtocol.scope 改对象形式 + deny 列表；ST-002/ST-003 退出线程跟踪。
3. **前端审查修复（FE-001~FE-015）**：15 个问题全部处理（14 修复 + 1 TODO 部分）。关键：FE-001 Bug #3；FE-002 appState 封装；FE-004 滑块防抖；FE-007 CSS 选择器注入修复；FE-008 骨架屏残留修复；FE-010 HTML 扩展名支持；FE-015 iframe sandbox。
4. **新增测试**：preview-modal.test.ts（14）、drag-drop.test.ts（11）、utils.test.ts（+11）、ipc.test.ts（+7）、wallpaper-list.test.ts（+2）；Rust ST-007（+11）、ST-012（+12）、FE-001（+2 集成测试）。
5. **验证结果**：Rust 374→400（+26，src-tauri 15→41），前端 86→151（+65），workspace 全量 **400 passed / 0 failed / 49 ignored**；clippy 0 warnings；fmt clean；npm test 151 passed；build success。**测试总数 460 → 551（+91）**。
6. **全局静态量演化**：v3.1 文档描述 7 个，v3.2 实测为 16 个（state.rs L109-195）。新增静态量主要用于线程跟踪与退出守卫，按用途分类为 A 类（Win32 回调必需）/ B 类（可收敛但风险高）/ C 类（退出守卫）。

---

## v3.2 → v3.3（2026-07-05）

基于 `cleanup-remaining-todos-2026-07-04` spec，清理 v3.2 遗留的 6 个 TODO 项，项目达到"零已知 TODO"状态。全部 6 项已完整修复（非简化方案）。

1. **ST-014 缩略图生成跟踪**：spawn_blocking 返回的 JoinHandle 保存到 `state::THUMBNAIL_TASK`；`perform_shutdown_blocking` 中 `take()` + 5s 超时等待。
2. **ST-015 文件对话框超时**：`open_file_dialog` 使用 `tokio::time::timeout(600s, ...)` 包装 `blocking_pick_file`。
3. **ST-016 CSP unsafe-inline 移除**：所有内联样式替换为 CSS 类。CSP 从 `style-src 'self' 'unsafe-inline'` 收紧为 `style-src 'self'`。
4. **ST-017 测试重构**：`validate_volume`/`validate_speed`/`parse_scaling_mode` 改为 `pub` + `lib.rs` re-export；5 处字符串匹配模拟测试重构为直接调用纯函数。
5. **WP-008 命名管道安全属性完整实现**（替代 v3.1 简化方案）：`build_pipe_security_attributes()` 通过 SDDL 构建安全描述符（`"D:P(A;;GA;;;BA)(A;;GA;;;SY)(A;;GA;;;<sid>)"`），DACL 仅允许 Administrators/SYSTEM/当前用户。`SecurityAttributesGuard` RAII 守卫。
6. **W-006 WebView2 创建超时**：`wait_with_pump_timeout`（PeekMessageA 轮询 + 30s 超时）替代无限阻塞的 `wait_for_async_operation`；`MirrorStarError` 新增 `WebView2Timeout(String)` 变体。
7. **工作区 Cargo.toml 重建**：会话期间曾被损坏（仅剩 2 行），已根据审查报告与源码分析完整重建，含 18 个 windows features 与所有依赖。
8. **验证结果**：cargo check/clippy（0 warnings，修复 2 个）/fmt/test（exit 0）/npm test（exit 0）/npm build（success）全部通过。测试数量与 v3.2 基线一致（400 Rust / 151 前端），本次为 TODO 清理迭代。

---

## v3.3 → v3.4（2026-07-07）

基于 `fix-wallpaper-preview-blank` + `fix-static-wallpaper-card-blank` 两个 spec，修复壁纸卡片预览空白的复合根因与 Image/Gif 静态壁纸卡片空白问题。按模块细分共 14 项修复。

1. **前端模块（3 项）**：缩略图加载失败 onerror 回退；Video 无缩略图时首帧预览；Image/Gif 无缩略图时直接加载源图片。
2. **后端 config 模块（3 项）**：Video 缩略图生成（ffmpeg）；损坏缩略图文件清理；mod.rs re-export 更新。
3. **后端 src-tauri 模块（4 项）**：批量重生成缩略图命令；ThumbnailFailedPayload 事件通知；add_wallpaper 扩展 Video 类型；启动清理钩子。
4. **前端 UI 模块（4 项）**：wallpaper-thumbnail-failed 事件监听；regenerateThumbnails IPC 包装；设置面板"重新生成缩略图"按钮；showStatus 扩展 info 类型。
5. **新增优化项**：16 项（config 3 + wallpaper 4 + src-tauri 4 + 前端 5）；新增 16 段 vibecoding 提示词；新增 16 项验收标准。

---

## v3.4 → v3.5（2026-07-12）

1. **深度模块审计**：完成 10 个模块的 8 维度深度代码审计（config / desktop / wallpaper / audio / ipc / process / src-tauri / wp-proc / 前端 UI / 构建基础设施）。
2. **8 维度**：正确性、健壮性、安全性、性能、可维护性、可测试性、并发安全、资源管理。
3. **findings 总数**：100 项（Critical 0 / **High 7** / Medium 36 / Low 57），与 v3.4 已识别 16 项 + 历史 fix-review spec 已修复项去重。
4. **关键 High 发现**：
   - config：load_config 静默回退默认配置（C01）
   - desktop：注册表写入无事务回滚（D01）
   - ipc：read_line OOM 防护失效（I02）
   - src-tauri：fullscreen.rs GetMessageW -1 误判（T02）
   - wp-proc：OpenProcessToken 句柄泄漏 + read_line OOM + WebView2 创建失败无反馈（WP01/WP02/WP03）
5. **文档更新**：第二~九章各追加 v3.5 findings 表格/实施方案/提示词/验收标准；新增附录 A.14。

---

## v3.5.1（2026-07-12）— High 级修复

基于 `fix-v35-high-findings-2026-07-12` spec，完成 7 项 High 级 findings 修复（C01/D01/I02/T02/WP01/WP02/WP03），v3.5 深度审计发现的全部 High 级问题清零。

1. **C01 config 解析失败通知前端**：ConfigManager 新增 `on_config_error` 回调 + `pending_config_errors` 延迟通知机制；src-tauri setup 注册回调，通过 `app.emit("config-load-error", error_message)` 通知前端；main.ts 监听事件展示 toast 告警。解析失败仍回退默认配置，应用可正常启动。
2. **D01 注册表事务回滚**：`set_native_wallpaper` 在 `set_wallpaper_style` 前读取旧注册表值，`set_wallpaper_image` 失败时恢复旧值并返回 Err。
3. **I02 IPC OOM 防护前置**：`read_line_with_timeout` 改用 `fill_buf()` + `consume()` 增量读取，每次填充后检查累计长度超限立即返回 Err，buf 内存永不超过 1MB。InfiniteByteStream 测试验证等价 100MB+ 攻击不 OOM。
4. **T02 GetMessageW -1 误判修复**：`fullscreen.rs` 消息循环从 `while GetMessageW(...).as_bool()` 改为 `loop { match ret.0 { 0 | -1 => break, _ => {} } }`，与 explorer.rs 一致。
5. **WP01 OpenProcessToken 句柄泄漏修复**：引入 `OwnedHandle` RAII 包装，Drop 时自动 `CloseHandle`，覆盖 `build_pipe_security_attributes` 所有返回路径。
6. **WP02 wp-proc read_line 限流**：新增 `read_line_with_limit` 函数使用 `fill_buf` + `consume` 增量读取，超限返回 `LineReadError::TooLong`，IPC 线程 continue 后继续监听不崩溃。82 个单元测试通过。
7. **WP03 WebView2 创建失败退出子进程**：`create_webview` 失败时返回 Err 并以退出码 1 退出子进程，不再 degraded 运行；`handle_command` controller 参数改为非 Option。
8. **验证结果**：cargo test --workspace 426 passed / 0 failed / 52 ignored；clippy 0 warnings；npm build 成功；npm lint 0 errors。

---

## v3.5.2（2026-07-13）— Medium 级修复

基于 `fix-v35-medium-findings-2026-07-13` spec，完成 36 项 Medium 级 findings 修复，v3.5 深度审计发现的全部 Medium 级问题清零。

1. **config 模块（C02-C06）**：validate() 范围校验；cleanup_corrupted_thumbnails 改用实例路径；generate_video_thumbnail 参数注入防护；is_ffmpeg_available 标注禁止 async；解压炸弹防护校验解码后像素缓冲区。
2. **desktop 模块（D02-D07）**：get_system_wallpaper 缓冲区扩大至 32767；set_mouse_passthrough 追加 SWP_FRAMECHANGED；embed_wallpaper 回退分支增加 warn；ensure_workerw_ready 失败条目移除；删除 find_workerw 死代码；SetWindowLongPtrW SetLastError/GetLastError 区分失败。
3. **wallpaper 模块（W01/W02/W04/W06/W07/W13）**：update_positions 移除负坐标 clamp；ImageRenderer pause/resume 发送命令；set_wallpaper play() 移出锁范围；pause_all_fast 消除 TOCTOU；WebRenderer::play spawn 监听任务；close_wallpaper_by_path 路径规范化。
4. **audio+ipc+process 模块（A01/I01/I03/P01）**：with_session GetProcessId 失败 continue；read_response_line_with_timeout 总体 deadline；send_command_with_timeout 总体 deadline；is_running 改用 WaitForSingleObject。
5. **src-tauri 模块（T01/T03/T04）**：set_speed fire-and-forget；pause/resume resolve_display_id 统一；perform_shutdown_blocking 3s 超时强制退出。
6. **wp-proc 模块（WP04-WP09）**：WebView2 回调失败先 tx.send；Pause/Resume CoreWebView2 失败返回 Error；SetPosition SetWindowPos 失败返回 Error；ComGuard RPC_E_CHANGED_MODE 返回 Err；create_webview GetClientRect 失败返回 Err；ipc_thread PostMessageW 失败构造错误响应。
7. **前端模块（F01-F04）**：内联 style 替换为 class（CSP 合规）；滑块 NaN 校验；loadConfig 失败 showStatus 提示；getErrorMessage 脱敏。
8. **构建模块（B01-B02）**：asset scope allow 收紧为 $APPDATA/mirrorstar/**/*；add_wallpaper 复制到受控目录；release.yml 配置 TAURI_SIGNING_PRIVATE_KEY。
9. **验证结果**：cargo test 全部通过；clippy 0 warnings；npm build 成功；npm lint 0 errors。

---

## v3.5.3（2026-07-14）— Low 级修复

基于 `fix-v35-low-findings-2026-07-13` spec，完成 57 项 Low 级 findings 修复。经 `review-v35-low-fixes-2026-07-14` 审查，34 项 PASS / 12 项 NEEDS_FIX / 11 项 FAIL；随后基于 `fix-v35-low-review-findings-2026-07-14` spec 修复剩余 23 项。至此 v3.5 深度审计 100 项 findings 全部修复完成。

1. **config 模块（C07-C11）**：atomic_write 清理 temp_path；load_config 消除 TOCTOU；update_thumbnail 路径规范化；start_periodic_save spawn 失败重置标志；start_watching 原子 reload。
2. **desktop 模块（D08-D12）**：枚举失败增加 warn；移除 dead store；提取魔法数字为常量；统一 callback 兄弟检查逻辑；restore 返回 Result。
3. **wallpaper 模块（W03/W05/W08-W12）**：decode_gif 实际帧尺寸计算 max_frames；提取 ensure_desktop_ready_impl 公共逻辑；register_window_class_once 区分错误码；gif_wallpaper_thread 取消检查点；set_speed 校验；VideoRenderer COM 失败降级；decode_gif 流式读取。
4. **audio+ipc+process 模块（A02-A03/I04-I05/P02-P05）**：set_process_volume clamp；with_session 清理失效缓存；send_command 15s 超时；connect_named_pipe 文档标注；WAIT_FAILED warn + 兜底；pid() is_running 检查；start/stop 文档标注；提取 wait_and_terminate。
5. **src-tauri 模块（T05-T16）**：THUMBNAIL_TASK 改 Vec；托盘 spawn_blocking；workerw_check emit 事件；validate_wallpaper_file_path symlink_metadata 逐级检查；set_gif_memory_strategy 内部加锁；标题缓冲区常量；EXPLORER_DESKTOP.set match；catch_unwind；GetSystemPowerStatus warn；check_and_reinitialize 返回 bool；set_wallpaper per-display 标志；主线程 ComGuard。
6. **wp-proc 模块（WP10-WP14）**：Terminate PostQuitMessage；Sleep 注释更新；read_line 重试 line.clear()；ShowWindow 移到成功后；default_rect 文档注释。
7. **前端模块（F05-F12）**：IntersectionObserver 懒渲染；iframe 移除 allow-scripts；外链 rel="noopener noreferrer"；空列表 showStatus；invokeWithTimeout；debounce unknown[]；beforeunload 提前注册；refreshWallpaperList 请求序号取消。
8. **构建模块（B03-B09）**：withGlobalTauri false；no-explicit-any warn；no-unused-vars error；npm audit；相对路径；coverage threshold；capabilities 最小权限。
9. **验证结果**：cargo test --workspace 全部通过（mirrorstar-core 396+3 / src-tauri 35 / wp-proc 88 / 0 failed / 24 ignored）；clippy 0 warnings（修复 8 个 lint）；npm build 成功；npm lint 0 errors；tsc --noEmit 通过；vitest 184 tests passed，覆盖率达标。

---

## 版本总览

| 版本 | 日期 | 核心内容 | 修复数 | 测试总数 |
|------|------|---------|--------|---------|
| v1.0-v2.0 | 2026-06-22 | 全面重新审计，48 项修复 | 48 | ~113 |
| v2.1 | 2026-06-23 | 剩余 10 项修复，修复率 100% | 10 | ~113 |
| v3.0 | 2026-07-03 | 代码复审 + 数据勘误 | — | ~113 |
| v3.1 | 2026-07-04 | v3.0 审查 24 项修复 | 24 | 460 |
| v3.2 | 2026-07-04 | src-tauri+前端深度审查 33 项 | 33 | 551 |
| v3.3 | 2026-07-05 | 6 项 TODO 清理，零已知 TODO | 6 | 551 |
| v3.4 | 2026-07-07 | 壁纸预览空白修复 14 项 | 14 | 551 |
| v3.5 | 2026-07-12 | 10 模块 8 维度深度审计 100 项 | — | — |
| v3.5.1 | 2026-07-12 | High 级修复 7 项 | 7 | 478 |
| v3.5.2 | 2026-07-13 | Medium 级修复 36 项 | 36 | — |
| v3.5.3 | 2026-07-14 | Low 级修复 57 项 | 57 | 584+ |
| **合计** | | | **231** | **584+** |

> v4.0 审查（2026-07-15）新增 125 项 findings（0 Critical / 18 High / 46 Medium / 61 Low），详见各模块文档"v4.0 审查发现"章节。

---

## v8.0 深度内存优化（2026-07-28）

- **spec**：`.trae/specs/deep-memory-optimization-v8-2026-07-28/`
- **背景**：设置动态壁纸后任务管理器显示 400MB+，深度研究定位 5 个根因
- **Wave v8-A**：WebView2 主窗口从 `hide()` 改为默认销毁，释放 150-300MB（`src-tauri/src/lib.rs`）
- **Wave v8-B**：新增 `JobObjectGuard`，mpv/wp-proc 子进程内存上限 256MB（`crates/mirrorstar-core/src/process/manager.rs` + `wallpaper/subprocess_base.rs`）
- **Wave v8-C**：GIF 流式帧解码，`DEFAULT_MAX_GIF_MEMORY_MB` 40→15，仅保留当前帧 ± 2 共 5 帧像素（`wallpaper/gif*.rs`）
- **Wave v8-D**：监控线程栈 2MB→512KB（`src-tauri/src/platform/fullscreen.rs` + `explorer.rs`）；tokio worker_threads 评估后跳过（Tauri v2 不支持）
- **Wave v8-E**：Image 首次绘制后释放像素数据，仅保留 HBITMAP（`wallpaper/gdi_cache.rs` + `gdi_base.rs` + `image.rs`）
- **验证**：cargo test 523+49 passed / cargo clippy 零警告 / npm test 202 passed
- **设计调整**：Wave v8-C 未新增 StreamingGifDecoder/StreamingFrameCache 独立结构体，改为 GifRenderData 方法 + decode_gif_frame_at 函数

---

## v9.0 性能优化（2026-07-28）

- **spec**：`.trae/specs/performance-optimization-v9-2026-07-28/`
- **背景**：v8.0 内存优化后深度研究发现 GIF 播放卡顿（v8-C 副作用）与网页壁纸冷启动慢（5-15s）
- **Wave v9-A**：GIF 后台预取，将 O(N) 帧解码移出主线程；新增 `decode_gif_frame_range` 批量解码 + `WM_GIF_FRAMES_PREFETCHED` 消息 + `prefetch_in_progress` 防重复（`wallpaper/gif_decode.rs` + `gif.rs`）
- **Wave v9-B**：wp-proc 预热启动池，应用启动时预初始化 WebView2 进程，网页壁纸启动 8s→<2s；新增 `WarmWpProc` + `play_with_warm` + `take_warm_wp_proc`/`refill_warm_pool`（`wallpaper/web.rs` + `manager.rs` + `state.rs` + `lib.rs` + `commands/wallpaper.rs`）（该机制后续因进程收束决策被彻底移除，见进程收束变更）
- **Wave v9-C**：IPC 重试间隔 200ms→50ms（mpv 30→120 次 / wp-proc 40→160 次，总超时不变 6s/8s）（`wallpaper/subprocess_base.rs`）
- **验证**：cargo test 539+57 passed / cargo clippy 零警告 / npm test 全绿
- **设计调整**：Wave v9-A 未实现 PersistentGifDecoder（image crate API 自引用限制），改为后台预取方案达成相同目标

---

## v10.0 内存优化（2026-07-28）

- **spec**：`.trae/specs/memory-optimization-v10-2026-07-28/`
- **背景**：v8.0 Job Object 设 256MB 上限防 OOM，但子进程实际占用仍偏高（mpv 默认缓存 ~75MB、WebView2 缓存无限制）
- **Wave v10-A**：mpv 添加 `--cache=no`/`--stream-cache=no`/`--vd-queue-max-bytes=16MB`/`--ad-queue-max-bytes=4MB`，节省 50-70MB（`wallpaper/video.rs`）
- **Wave v10-B**：WebView2 改用 `CreateCoreWebView2EnvironmentWithOptions` + UserDataFolder + `--disk-cache-size=10MB`，节省 20-50MB（`wp-proc/webview.rs`）
- **Wave v10-C**：GIF 预取 `MAX_PREFETCH_FRAME_BYTES=8MB` 跳过超大帧 + 显式 drop，节省 5-10MB 瞬时峰值（`wallpaper/gif_decode.rs` + `gif.rs`）
- **验证**：cargo test 542+57 passed / cargo clippy 零警告 / npm typecheck 通过
- **降级路径**：Wave v10-B options/folder 构建失败时回退到 `CreateCoreWebView2Environment`（默认行为）

---

## v11.0 内存优化（2026-07-28）

- **spec**：`.trae/specs/memory-optimization-v11-2026-07-28/`
- **背景**：v10.0 降低子进程实际占用后，深度审查发现 4 个未被覆盖的优化机会（mpv demuxer 读取缓冲、WebView2 GPU/字体缓存、4K/8K 屏 ImageRenderer 像素过多、Tokio runtime 默认 worker_threads 过多）
- **Wave v11-A**：mpv 追加 `--demuxer-max-bytes=0`/`--demuxer-max-back-bytes=0`/`--cache-secs=0`，彻底清零 demuxer 读取缓冲（默认 50MB+30MB+1s），节省 5-15MB（`wallpaper/video.rs`）
- **Wave v11-B**：WebView2 追加 `--disable-gpu-program-cache`/`--disable-gpu-shader-disk-cache`，`--disable-features` 扩展为 `MediaCache,FontCache,GpuMemDiscardable,BackForwardCache`，节省 5-10MB（`wp-proc/webview.rs`）
- **Wave v11-C**：ImageRenderer 新增 `MAX_DOWNSAMPLE_DIMENSION=2560`（QHD），4K/8K 屏降采样目标改为 `min(screen_w, 2560)` × `min(screen_h, 2560)`，节省 4K ~18MB / 8K ~118MB（`wallpaper/image.rs`）
- **Wave v11-D**：自定义 Tokio runtime，`worker_threads=2` + `thread_stack_size=512KB` + `max_blocking_threads=8`，替代默认 `num_cpus` worker（8 核 16MB→1MB），节省 ~15MB（`src-tauri/src/lib.rs`）
- **验证**：cargo test --workspace --lib 全绿（237 wallpaper tests passed）/ cargo clippy 零警告 / npm typecheck 通过
- **新增测试**：`test_downsample_caps_at_max_dimension` 验证 4K/8K/1080p 三种屏幕场景的降采样上限行为
- **待手动验证**：mpv.exe 内存 < 170MB、wp-proc.exe 内存 < 190MB、4K 图片壁纸主进程增量 < 20MB、Tokio worker threads = 2（详见 `10-实施路线图.md §10.13.5`）
- **核心目标**：v10.0 已降低子进程内存，v11.0 进一步降低子进程内存并**首次优化主进程内存**（Tokio runtime + ImageRenderer 4K 屏场景）

---

## v12.0 内存优化（2026-07-29）

- **spec**：`.trae/specs/memory-optimization-v12-2026-07-29/`
- **背景**：v8.0-v11.0 充分覆盖峰值内存后，深度审查发现 2 个长期运行泄漏（前端 `lowercasedFileNameCache` 不清理删除条目 + 后端 `session_cache` 进程退出后不清除）
- **Wave v12-A**：前端 `removeWallpaperCard` splice 后同步 `lowercasedFileNameCache.delete(file_path)`，新增 `__getCacheSizeForTests()` 测试辅助（`src/scripts/ui/wallpaper-list.ts`）
- **Wave v12-B**：前端 `refreshWallpaperList` 在 `prefetchFileNameCache` 前先 `lowercasedFileNameCache.clear()`，覆盖 file_path 重命名场景（`src/scripts/ui/wallpaper-list.ts`）
- **Wave v12-C**：后端新增 `HandleGuard` RAII + `is_process_alive(pid)` 辅助函数（`OpenProcess` + `GetExitCodeProcess` + `STILL_ACTIVE`），`with_session_once` 失败路径检查进程存活并清理 `session_cache`（`crates/mirrorstar-core/src/audio/volume.rs`）
- **验证**：cargo test --workspace --lib 全绿 / cargo clippy 零警告 / npm typecheck 通过 / npm test 206 passed（+4 新增前端测试）/ npm lint 零警告
- **新增测试**：前端 4 个（删除清理 / 不影响其他条目 / 重命名场景清理 / 空列表清空）+ 后端 3 个（dead PID / current PID / 失败路径清理不 panic）
- **待手动验证**：连续增删 50 次壁纸后 `lowercasedFileNameCache.size` ≤ 当前壁纸数、连续切换 20 次视频壁纸后 `session_cache` 不含已退出 PID（详见 `10-实施路线图.md §10.14.5`）
- **核心目标**：v8.0-v11.0 已覆盖峰值内存优化，v12.0 聚焦**长期运行泄漏修复**，确保应用连续运行数小时后内存不持续增长

---

## v13.0 内存优化（2026-07-29）

- **spec**：`.trae/specs/memory-optimization-v13-2026-07-29/`
- **背景**：v8.0-v12.0 已系统覆盖峰值内存与长期运行泄漏。本次为第二轮深度审查（前端/后端核心库/wp-proc/Tauri 应用层），仅发现 1 项状态一致性问题
- **Wave v13-A**：`close_wallpaper` 追加 `wallpaper_scaling_modes.remove(display_id)`，与 `pause_senders`/`wallpaper_mode`/`wallpaper_sources`/`wallpapers` 4 个 HashMap 的清理保持一致（`crates/mirrorstar-core/src/wallpaper/manager.rs`）
- **Wave v13-B**：`cleanup_internal` 追加 `wallpaper_scaling_modes.clear()`，与既有 `pause_senders.clear()` 对称（shutdown 路径 safety net）
- **修复理由**：避免 W-008 `set_scaling_mode` 失败回退时使用已关闭壁纸的残留 scaling mode，关闭壁纸后回退目标重置为默认值 Fill
- **验证**：cargo test --workspace --lib 全绿 / cargo clippy --workspace --all-targets 零警告
- **新增测试**：2 个（`v13_close_wallpaper_clears_scaling_modes` + `v13_cleanup_internal_clears_scaling_modes`）
- **深度审查结论**：v8-v12 已覆盖绝大部分有意义的内存优化机会，剩余模式（lazyObserver/cardData WeakMap/cleanups/pendingAdds/pending_config_errors/THUMBNAIL_TASK/IPC partial/TmpFrameGuard/ControllerGuard/Tokio runtime/wpfile 协议/监控线程 join 等）均已被充分处理或本就不是泄漏
- **核心目标**：v13.0 是 v8-v12 内存优化系列的**收尾 spec**，修复深度审查发现的最后一项状态一致性问题，形成 v8→v13 完整的内存优化审计闭环

---

## v14.0 第三轮深度内存审查收尾（2026-07-29）

- **spec**：`.trae/specs/memory-optimization-v14-2026-07-29/`
- **背景**：v13.0 收尾后的第三轮深度审查，验证 v8-v13 是否已覆盖所有有意义的内存优化机会
- **代码改动**：**无任何代码改动**（纯文档收尾 spec）
- **审查范围**：四个维度逐文件扫描——前端（`src/scripts/**`）/ 后端核心库（`crates/mirrorstar-core/src/**`）/ wp-proc（`crates/mirrorstar-wp-proc/src/**`）/ Tauri 应用层（`src-tauri/src/**`）
- **审查方法论**：四维度逐文件扫描 + 已覆盖项交叉验证，每个潜在泄漏点标注"已覆盖 spec"或"本就不是泄漏"
- **审查结论**：**零新发现**——v8-v13 已形成完整的内存优化审计闭环
  - 前端 12 项审查：8 项已正确处理 + 4 项本就不是泄漏
  - 后端核心库 18 项审查：12 项已正确处理 + 5 项本就不是泄漏 + 1 项 v13.0 已覆盖
  - wp-proc 6 项审查：1 项 v3.5.1 已覆盖 + 5 项本就不是泄漏
  - Tauri 应用层 12 项审查：5 项已正确处理 + 5 项本就不是泄漏 + 2 项 v11/v12 已评估
- **文档更新**：`docs/05-优化文档/10-实施路线图.md` 新增 §10.16（4 个小节：背景 / 方法论 / 各维度结论 / 系列收尾声明）
- **核心目标**：v14.0 是内存优化系列的**纯文档收尾 spec**，通过第三轮深度审查的零发现结论，确认 v8-v13 已形成完整的内存优化审计闭环。后续如无新功能引入新状态字段，无需再启动内存优化 spec
